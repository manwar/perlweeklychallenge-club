⍝ This function solves the actual challenge
Solve2 ⇐ {
	∊ (¯1↓1↓)¨ ⍵ ⊆⍨ ¯1↓1,0=+\¯1+2×@(=⍵
}

⍝ Compare the results with all the examples in the description
TestSolution2 ⇐ {
	results ← `
		(""           ≡ Solve2 "()()()") `
		("((()))"     ≡ Solve2 "(((())))") `
		("()()()"     ≡ Solve2 "(()())(())") `
		("(())"       ≡ Solve2 "()((()))()") `
		("()(())()()" ≡ Solve2 "(()(()))(()())")
	∧/results ⍝ Return true if all results are correct
}
