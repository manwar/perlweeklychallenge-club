# Perl Weekly Challenge 126 Solutions

Perl and Python implementations for both tasks of Weekly Challenge 126.

## Task 1: Count Numbers

Count numbers in `1..N` that do not contain digit `1`.

- **Perl**: `perl/ch-1.pl` (existing)
- **Python**: `python/ch-1.py`

## Task 2: Minesweeper Game

Convert a minefield (`x` for mines, `*` for empty) into a minesweeper board
with adjacent mine counts.

- **Perl**: `perl/ch-2.pl` (existing)
- **Python**: `python/ch-2.py`

## Running

- **Perl**: `perl perl/ch-1.pl` or `perl perl/ch-2.pl`
- **Python**: `python3 python/ch-1.py` or `python3 python/ch-2.py`
