# Perl Weekly Challenge 103 Solutions

Perl and Python implementations for both tasks of Weekly Challenge 103.

## Task 1: Chinese Zodiac

Given a year, determine the Chinese Zodiac (element + animal).

- **Perl**: `perl/ch-1.pl` (existing)
- **Python**: `python/ch-1.py`

## Task 2: What's playing?

Given a start time, current time and a CSV playlist (milliseconds + title),
output which track is currently playing and the position within it.

- **Perl**: `perl/ch-2.pl` (existing; uses `perl/now_playing.csv` for the example test)
- **Python**: `python/ch-2.py`

## Running

- **Perl**: `perl perl/ch-1.pl` or `perl perl/ch-2.pl`
- **Python**: `python3 python/ch-1.py` or `python3 python/ch-2.py`
